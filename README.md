# Joey Marra Personal Site

The standalone Next.js + Tailwind portfolio for `joeymarra.com`.

## Local development

```bash
npm install
npm run dev
```

Deploy this `personal_site` directory as the Vercel project root. The production command is `npm run build`.
